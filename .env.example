# ComfyUI Grid Generator - Environment Configuration Example
# Copy this file to .env and customize values as needed
# Note: .env is gitignored by default

# CLI menu defaults for CSV converters (optional)
# Used when selecting convert_x_csv / convert_y_csv and leaving "Extra argv" empty
# CONVERT_X_DEFAULT_CSV=data/prompts/X/common_prompts.csv
# CONVERT_Y_DEFAULT_CSV=data/prompts/Y/300_NAI_Styles_Table-test.csv

# X axis JSON file path (default: data/prompts/X/common_prompts.json)
# JSON file path for X-axis prompts configuration
COMFYUI_X_JSON=data/prompts/X/common_prompts.json

# Y axis JSON file path (default: data/prompts/Y/300_NAI_Styles_Table-test.json)
# JSON file path for Y-axis prompts configuration
COMFYUI_Y_JSON=data/prompts/Y/300_NAI_Styles_Table-test.json

# Prompt template (optional; default uses built-in order)
# Only supports placeholder tokens concatenation, e.g.
#   {gender}{characters}{series}{rating}{y}{general}{quality}
#   {gender}{y}{quality}
# COMFYUI_TEMPLATE={gender}{characters}{series}{rating}{y}{general}{quality}

# Base seed (optional; default: 0)
# COMFYUI_BASE_SEED=0

# Optional: explicitly choose KSampler node id in workflow
# COMFYUI_KSAMPLER_NODE_ID=3

# Optional: select subset of X/Y rows
# COMFYUI_X_LIMIT=10
# COMFYUI_Y_LIMIT=10
# COMFYUI_X_INDEXES=0,1,2,3
# COMFYUI_Y_INDEXES=0,5,9

# Optional: use fixed run dir instead of auto timestamped dir under COMFYUI_OUT_DIR
# COMFYUI_RUN_DIR=comfyui_api_outputs/my-run

# Optional: enable dry-run via env (1/0 true/false yes/no on/off)
# COMFYUI_DRY_RUN=1

# ComfyUI server base URL (default: http://127.0.0.1:8188)
# Adjust if running ComfyUI on a different host or port
COMFYUI_BASE_URL=http://127.0.0.1:8188

# Path to ComfyUI workflow JSON file (default: data/comfyui-flow/CKNOOBRF.json)
# Must be a valid ComfyUI workflow with CLIPTextEncode nodes
COMFYUI_WORKFLOW_JSON=data/comfyui-flow/CKNOOBRF.json

# Output directory root (default: comfyui_api_outputs)
# Generated run directories will be created under this path
COMFYUI_OUT_DIR=comfyui_api_outputs

# Client ID for ComfyUI WebSocket connection (default: auto-generated UUID)
# Optional: specify a custom client ID for debugging
# COMFYUI_CLIENT_ID=my-custom-client-id

# HTTP request timeout in seconds (default: 30.0)
# Timeout for individual HTTP requests to ComfyUI
COMFYUI_REQUEST_TIMEOUT_S=30.0

# Job timeout in seconds (default: 600.0)
# Maximum time to wait for a single prompt generation
COMFYUI_JOB_TIMEOUT_S=600.0

# Local concurrency (default: 1)
# Controls how many prompts are kept in flight; higher values can reduce idle time caused by image downloads.
COMFYUI_CONCURRENCY=1

# Negative prompt to override workflow default (optional)
# If not set, uses the default from workflow JSON
# COMFYUI_NEGATIVE_PROMPT=low quality, blurry, bad anatomy

# Optional: additional negative prompt appended to normal-type X entries
# Only applies when x_info_type == "normal"
# Default value (if unset): nsfw, nipples, pussy, nude,
# Behavior:
#   - Unset/missing: uses default string above
#   - Set to empty string or whitespace: disables append
#   - Set to custom value: uses your value
# COMFYUI_APPEND_NEGATIVE_PROMPT=nsfw, nipples, pussy, nude,

# Generation parameters (optional overrides for workflow defaults)
# If not set, uses values from workflow JSON

# Image dimensions (pixels)
COMFYUI_WIDTH=512
COMFYUI_HEIGHT=512

# Batch size (number of images to generate per prompt)
COMFYUI_BATCH_SIZE=1

# Sampling steps (higher = better quality but slower)
COMFYUI_STEPS=20

# CFG scale (classifier-free guidance scale, typically 7.0-12.0)
COMFYUI_CFG=7.0

# Denoise strength (0.0-1.0, 1.0 = full regeneration)
COMFYUI_DENOISE=1.0

# Sampler name (e.g., euler, euler_a, ddim, uni_pc)
COMFYUI_SAMPLER_NAME=euler

# Scheduler (e.g., normal, karras, exponential, sgm_uniform)
COMFYUI_SCHEDULER=normal

# ============================================================================
# R2 Upload & Supabase Configuration (execute mode only)
# ============================================================================
# The following variables are required when running in execute mode (--dry-run not needed)
# See provisioning guides: scripts/r2_upload/r2_provisioning.md, scripts/r2_upload/supabase_workflow.md
# IMPORTANT: Real credentials go in .env only (gitignored), never commit .env

# Cloudflare R2 S3 API endpoint
# Format: https://<ACCOUNT_ID>.r2.cloudflarestorage.com
# Get ACCOUNT_ID from: https://dash.cloudflare.com -> R2 -> Account ID
# R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com

# Cloudflare R2 API Token credentials
# Create at: https://dash.cloudflare.com/profile/api-tokens
# Permissions needed: R2:Edit, Object Storage:Read & Write
# R2_ACCESS_KEY_ID=your-access-key-id-here
# R2_SECRET_ACCESS_KEY=your-secret-access-key-here

# Cloudflare R2 bucket names
# Create buckets using: wrangler r2 bucket create <bucket-name>
# Public bucket: stores 'normal' category display/thumb variants (publicly accessible)
# Private bucket: stores all original PNG images and advance/nsfw variants
# R2_PUBLIC_BUCKET=sdslab-public
# R2_PRIVATE_BUCKET=sdslab-private

# Optional: directory for processed image intermediate artifacts before upload
# If unset, uploader defaults to <run-root>/_r2_upload_intermediate
# Use this to avoid high memory usage and to rerun upload without re-encoding images
# R2_UPLOAD_INTERMEDIATE_DIR=/absolute/or/relative/path/to/intermediate

# Optional: Public base URL for direct object access (e.g., r2.dev or custom domain)
# If using r2.dev: https://<R2_PUBLIC_BUCKET>.<ACCOUNT_ID>.r2.cloudflarestorage.com
# If using custom domain: https://your-custom-domain.com
# R2_PUBLIC_BASE_URL=https://sdslab-public.your-account-id.r2.cloudflarestorage.com

# ============================================================================
# Website (Next.js) - Public Runtime Configuration
# ============================================================================
# Supabase SSR uses these NEXT_PUBLIC_* variables in both browser and server code.
# Keep values empty here; real values go in .env only.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Public base URL for reading public images (no trailing "/")
NEXT_PUBLIC_R2_PUBLIC_BASE_URL=

# Supabase connection credentials
# Create project at: https://app.supabase.com
# Service role key has full bypass of RLS (use only for trusted backend scripts)
# SUPABASE_URL=https://your-project-ref.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Supabase uploader performance knobs
# Batch size for upsert payload rows (must be >= 1)
# SUPABASE_UPSERT_BATCH_SIZE=100

# Max request body size in bytes for each upsert chunk (must be >= 1024)
# SUPABASE_UPSERT_MAX_BYTES=4000000

# Parallel workers for image_variants batch writes (must be >= 1)
# SUPABASE_DB_CONCURRENCY=2

# HTTP timeout (seconds) for PostgREST requests
# SUPABASE_HTTP_TIMEOUT_S=30

# Retries for transient PostgREST transport/server errors (0 = no retry)
# SUPABASE_HTTP_RETRIES=2

# ============================================================================
# Image Encoding & Upload Parameters (planned / not wired yet)
# ============================================================================
# These parameters control image variant generation quality and concurrency
# NOTE: Values below are defaults used by the uploader (not yet wired to code)
# When implemented, they will be read from these environment variables

# WebP quality levels (1-100, higher = better quality, larger file)
# R2_WEBP_LARGE_QUALITY=93  # L1: full-size display variant
# R2_WEBP_THUMB_QUALITY=82  # L2: 1/2 size thumbnail variant

# AVIF CRF approximation (not direct Pillow parameter, mapped internally)
# Lower = better quality, larger file; mapping documented in code
# R2_AVIF_LARGE_CRF=20      # L1: full-size display variant
# R2_AVIF_THUMB_CRF=28      # L2: 1/2 size thumbnail variant

# Blurhash component count (width x height, affects hash precision vs length)
# Recommended values: 3x3, 4x3, 4x4 (higher = more components, longer hash)
# R2_BLURHASH_COMPONENTS=4x3

# Worker count for image planning/encoding thread pool (default: CLI --concurrency)
# Set based on CPU cores; e.g. 4, 8, 16. Must be >= 1.
# R2_IMAGE_WORKERS=4

# Worker count for R2 upload stage (default: 1)
# Controls concurrent image object uploads to better utilize network bandwidth.
# Must be >= 1.
# R2_UPLOAD_CONCURRENCY=8
