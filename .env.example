# ComfyUI Grid Generator - Environment Configuration Example
# Copy this file to .env and customize values as needed
# Note: .env is gitignored by default

# CLI menu defaults for CSV converters (optional)
# Used when selecting convert_x_csv / convert_y_csv and leaving "Extra argv" empty
# CONVERT_X_DEFAULT_CSV=data/prompts/X/common_prompts.csv
# CONVERT_Y_DEFAULT_CSV=data/prompts/Y/300_NAI_Styles_Table-test.csv

# X axis JSON file path (default: data/prompts/X/common_prompts.json)
# JSON file path for X-axis prompts configuration
COMFYUI_X_JSON=data/prompts/X/common_prompts.json

# Y axis JSON file path (default: data/prompts/Y/300_NAI_Styles_Table-test.json)
# JSON file path for Y-axis prompts configuration
COMFYUI_Y_JSON=data/prompts/Y/300_NAI_Styles_Table-test.json

# Prompt template (optional; default uses built-in order)
# Only supports placeholder tokens concatenation, e.g.
#   {gender}{characters}{series}{rating}{y}{general}{quality}
#   {gender}{y}{quality}
# COMFYUI_TEMPLATE={gender}{characters}{series}{rating}{y}{general}{quality}

# Base seed (optional; default: 0)
# COMFYUI_BASE_SEED=0

# Optional: explicitly choose KSampler node id in workflow
# COMFYUI_KSAMPLER_NODE_ID=3

# Optional: select subset of X/Y rows
# COMFYUI_X_LIMIT=10
# COMFYUI_Y_LIMIT=10
# COMFYUI_X_INDEXES=0,1,2,3
# COMFYUI_Y_INDEXES=0,5,9

# Optional: use fixed run dir instead of auto timestamped dir under COMFYUI_OUT_DIR
# COMFYUI_RUN_DIR=comfyui_api_outputs/my-run

# Optional: enable dry-run via env (1/0 true/false yes/no on/off)
# COMFYUI_DRY_RUN=1

# ComfyUI server base URL (default: http://127.0.0.1:8188)
# Adjust if running ComfyUI on a different host or port
COMFYUI_BASE_URL=http://127.0.0.1:8188

# Path to ComfyUI workflow JSON file (default: data/comfyui-flow/CKNOOBRF.json)
# Must be a valid ComfyUI workflow with CLIPTextEncode nodes
COMFYUI_WORKFLOW_JSON=data/comfyui-flow/CKNOOBRF.json

# Output directory root (default: comfyui_api_outputs)
# Generated run directories will be created under this path
COMFYUI_OUT_DIR=comfyui_api_outputs

# Client ID for ComfyUI WebSocket connection (default: auto-generated UUID)
# Optional: specify a custom client ID for debugging
# COMFYUI_CLIENT_ID=my-custom-client-id

# HTTP request timeout in seconds (default: 30.0)
# Timeout for individual HTTP requests to ComfyUI
COMFYUI_REQUEST_TIMEOUT_S=30.0

# Job timeout in seconds (default: 600.0)
# Maximum time to wait for a single prompt generation
COMFYUI_JOB_TIMEOUT_S=600.0

# Local concurrency (default: 1)
# Controls how many prompts are kept in flight; higher values can reduce idle time caused by image downloads.
COMFYUI_CONCURRENCY=1

# Negative prompt to override workflow default (optional)
# If not set, uses the default from workflow JSON
# COMFYUI_NEGATIVE_PROMPT=low quality, blurry, bad anatomy

# Optional: additional negative prompt appended to normal-type X entries
# Only applies when x_info_type == "normal"
# Default value (if unset): nsfw, nipples, pussy, nude,
# Behavior:
#   - Unset/missing: uses default string above
#   - Set to empty string or whitespace: disables append
#   - Set to custom value: uses your value
# COMFYUI_APPEND_NEGATIVE_PROMPT=nsfw, nipples, pussy, nude,

# Generation parameters (optional overrides for workflow defaults)
# If not set, uses values from workflow JSON

# Image dimensions (pixels)
COMFYUI_WIDTH=512
COMFYUI_HEIGHT=512

# Batch size (number of images to generate per prompt)
COMFYUI_BATCH_SIZE=1

# Sampling steps (higher = better quality but slower)
COMFYUI_STEPS=20

# CFG scale (classifier-free guidance scale, typically 7.0-12.0)
COMFYUI_CFG=7.0

# Denoise strength (0.0-1.0, 1.0 = full regeneration)
COMFYUI_DENOISE=1.0

# Sampler name (e.g., euler, euler_a, ddim, uni_pc)
COMFYUI_SAMPLER_NAME=euler

# Scheduler (e.g., normal, karras, exponential, sgm_uniform)
COMFYUI_SCHEDULER=normal
